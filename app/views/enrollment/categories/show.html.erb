<p id="notice"><%= notice %></p>
<table>
<thead>
<tr>
  <th><strong>이수구분</strong></th>
   <th><strong>과목명</strong></th>
  <th><strong>과목 정보</strong></th>
</tr>
</thead>
  <% @categories.each do |category| %>
  <% @lecture.each do |lecture| %>
<tbody>
  <tr>
    <td><%= @category.name %></td>
  </tr>
  <tr>
    <td><%= @lecture.name %></td>
  </tr>
  <tr>
    <td><%= @lecture.info %></td>
  </tr>
</tbody>
  <% end %>
<% end %>
</table>
<%= link_to '수정', edit_enrollment_category_path(@category) %> |
<%= link_to '뒤로가기', enrollment_categories_path %>
